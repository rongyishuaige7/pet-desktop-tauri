{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default app permissions for the studio and desktop pet windows.",
  "windows": ["studio"],
  "permissions": ["core:default", "core:window:allow-start-dragging"]
}
